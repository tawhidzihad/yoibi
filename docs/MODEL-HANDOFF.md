# YOIBI Model Handoff

This file prevents a new AI model from restarting work from zero.

## Rule
At the end of every meaningful session/task, the active model must update this file. A new model must read it before changing code.

## Current Snapshot
- Last updated: 2026-09-11
- Active task: Phase 5 — Milestone 9: Admin Dashboard & Moderation Tools
- Overall phase: Phase 5 — Milestone 9 FULLY IMPLEMENTED & VERIFIED (Quality Gates 100% Passed)
- Git repository status: Initialized at `yoibi/` root
- Current branch: `main`
- Last completed milestone: **Phase 5 — Milestone 9: Admin Dashboard & Moderation Tools**
  1. **Canonical Identity Model:** Better Auth User ID (`String`) mapped 1:1 with MongoDB `User._id`. Used consistently across all domain collections (`Tweet`, `Video`, `Stream`, `MeetUp`, `Follow`, `Message`, `Notification`, `Report`, `AuditLog`). MongoDB autogenerated ObjectIds are never used for user identity.
  2. **Better Auth Authentication Authority:** Better Auth `better-auth` v1.7.4 with official `admin()` plugin (`auth.api.banUser`, `auth.api.unbanUser`, `auth.api.removeUser`, `auth.api.revokeUserSessions`). Zero direct database mutations on undocumented internal collections.
  3. **Strict Block vs Ban Semantics (Naming Distinction):**
     - **YOIBI Block:** Reversible suspension via Better Auth `banUser()` + session revocation, setting `User.isBlocked: true`. Data 100% preserved; user redirected to `/account-blocked`.
     - **YOIBI Ban:** Irreversible permanent data purge via Better Auth `removeUser()` plus the canonical 5-phase data purge.
     - **Explicit Rule:** `Better Auth banUser != YOIBI permanent Ban`.
  4. **Durable Ban State Machine & Pre-Cleanup Audit Record:**
     - Audit log initialized in MongoDB before any destruction (`action: 'BAN_USER'`, `status: 'REQUESTED'`).
     - States: `REQUESTED` -> `IN_PROGRESS` -> `COMPLETED` / `PARTIAL` / `FAILED`.
     - `FAILED`: A critical failure prevented the Ban from continuing, remaining recoverable and resumable.
     - Per-Resource External Tracking: `externalSnapshots` tracks `status: pending|completed|failed` and `error` for every individual Cloudinary asset and LiveKit room.
  5. **Canonical 5-Phase Ban Policy & 9-Stage Technical Mapping:**
     - Canonical Phase A (Validation & Protection) -> Implementation Stage A
     - Canonical Phase B (External Teardown) -> Implementation Stages B-E (LiveKit termination, Cloudinary purge)
     - Canonical Phase C (App Data Purge & Anonymization) -> Implementation Stage F (Tweets, replies count repair, Videos, Streams, Meet-Ups, bidirectional Follows, senderId anonymization to `deleted_user`, notifications)
     - Canonical Phase D (Account & Identity Removal) -> Implementation Stages G-H (Better Auth removeUser, MongoDB User document purge)
     - Canonical Phase E (Audit Finalization) -> Implementation Stage I (Final status, deletedCounts, completion timestamp)
  6. **Dashboard Metrics:** Aggregated current counts (`currentUsers`, `activeUsers`, `blockedUsers`, `bannedUsers`, `totalTweets`, `totalVideos`, `liveStreams`, `activeMeetUpRooms`, `pendingReports`).
  7. **Reports System:** Full lifecycle (`pending`, `resolved`, `dismissed`), resolution notes, and permanent preservation across user bans.
  8. **Frontend Admin Feature & UX:**
     - Feature folder: `frontend/src/features/admin/` with custom hooks (`useAdminStats`, `useAdminUsers`, `useAdminReports`, `useAdminContent`, `useAdminAuditLogs`) and UI components (`StatsOverview`, `UserTable`, `UserDetailModal`, `BlockUserModal`, `UnblockUserModal`, `BanUserModal`, `ReportsQueue`, `ReportDetailModal`, `ContentModerator`, `AuditLogViewer`).
     - Dedicated page routes: `/admin`, `/admin/users`, `/admin/content`, `/admin/reports`, `/admin/audit-logs`.
     - Account blocked page: `frontend/src/app/(auth)/account-blocked/page.js`.
     - Protected layout navigation and `isBlocked` redirection.
  9. **Quality Gates Passed:**
     - Backend tests (`npm test`): 100% passing across all 8 test suites (Foundation, Tweets, Videos, Streams, Meet-Up, Messaging, Notifications, Admin).
     - Backend ESLint (`npm run lint`): 0 errors, 0 warnings.
     - Frontend ESLint (`npm run lint`): 0 errors, 0 warnings.
     - Frontend Next.js production build (`npm run build`): Clean compilation with 21 static/dynamic optimized routes.
- Exact next milestone: **Phase 5 — Milestone 10: Platform Hardening, End-to-End Polish & Deployment Readiness**.

## What Is Working
- Better Auth server & client integration in Next.js (`frontend/src/lib/auth.js`, `frontend/src/lib/auth-client.js`, `/api/auth/[...all]`).
- JWT acquisition and transport via `authClient.getJwtToken()` and `Authorization: Bearer <token>` header in `apiClient`.
- Centralized API clients (`frontend/src/lib/api/admin.js`, `reports.js`, `client.js`).
- Profile endpoints, Follow endpoints, Wall view.
- **Tweet & Feed Domain:** Mongoose `Tweet` model, repository, services, controllers, routes, tests, and frontend components.
- **Videos Domain:** Cloudinary upload intent integration, `Video` model, repository, services, controllers, routes, and frontend video player/feed.
- **Streams Domain:** LiveKit SFU broadcast integration, `Stream` model, lifecycle state machine, and frontend live broadcast viewer/host suite.
- **Meet-Up Domain:** LiveKit collaborative multi-peer rooms, atomic capacity reservations, and dynamic grid layouts.
- **Messages Domain:** Follow-gated permissions, canonical conversation grouping, message idempotency, Socket.IO gateway.
- **Notifications Domain:** Real-time social alerts, Better Auth String ID identity, duplicate prevention & undo cleanup, unread counters.
- **Admin & Moderation Domain:** Overview metrics, user management, reversible block/unblock, permanent 5-phase/9-stage Ban orchestrator, content moderation browser & direct deletion, reports queue & resolution, audit log explorer with external snapshot telemetry, `/account-blocked` page.
- All backend and frontend linting and production builds pass cleanly with 0 errors, 0 warnings.

## What Is Not Working / Remaining Scope
- Ready for Milestone 10 (End-to-End polish and deployment readiness).

## Tests/Checks Run
- Backend tests (`npm test`): Passed 100% across all 8 test suites
- Backend ESLint (`npm run lint`): 0 errors, 0 warnings
- Frontend ESLint (`npm run lint`): 0 errors, 0 warnings
- Frontend build (`npm run build`): Compiled cleanly (21 routes)
- Indentation check: 4-space indentation across all modified files
- Tab check: Zero tab characters across `frontend/src` and `backend/src`

## Exact Resume Instruction
> Phase 5 — Milestone 9 (Admin Dashboard & Moderation Tools) complete. Proceed to Phase 5 — Milestone 10 (Platform Hardening & Deployment Readiness).
