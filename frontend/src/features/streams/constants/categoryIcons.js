import {
    Landmark,
    Newspaper,
    BookOpen,
    Building2,
    PartyPopper,
    MessageSquare,
    Mic,
    Tv
} from "lucide-react";

/**
 * Icon registry for the 8 canonical YOIBI stream categories.
 * Keys mirror the `icon` field in CANONICAL_CATEGORIES.
 */
export const CATEGORY_ICONS = {
    Landmark,
    Newspaper,
    BookOpen,
    Building2,
    PartyPopper,
    MessageSquare,
    Mic,
    Tv
};
