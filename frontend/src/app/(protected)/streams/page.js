import { StreamsView } from "../../../features/streams/ui/StreamsView";

export const metadata = {
    title: "Live Streams",
    description: "Watch and start live streams on Yoibi.",
};

export default function StreamsPage() {
    return <StreamsView />;
}
