# Git/GitHub Skill

Rules:
- Keep `frontend/` and `backend/` independent in their package/deployment concerns.
- Inspect git status before and after a task.
- Keep commits focused.
- Do not commit `.env`, credentials, tokens, or private keys.
- Preserve useful legacy GitHub Actions knowledge, but redesign CI for the new frontend/backend split.
