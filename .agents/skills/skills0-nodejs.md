# Node.js Skill

Source of truth: https://nodejs.org/docs/latest/api/

YOIBI backend rules:
- Use Node.js with CommonJS.
- Use environment variables for runtime configuration.
- Handle startup failures clearly.
- Gracefully handle process-level errors without hiding the root cause.
- Avoid reading large uploads fully into memory when a streaming/chunked solution is more appropriate.
