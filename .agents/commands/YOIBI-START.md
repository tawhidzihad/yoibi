# YOIBI Start Command

Use this instruction when beginning the project with a fresh AI model.

Run the rules in `START-PROMPT.md` exactly. Do not code before reading the mandatory docs and inspecting the legacy project.
